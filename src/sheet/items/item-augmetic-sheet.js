import ImpMalItemSheet from "./item-sheet";

export default class AugmeticItemSheet extends ImpMalItemSheet
{
    static get defaultOptions() 
    {
        const options = super.defaultOptions;
        return options;
    }
}