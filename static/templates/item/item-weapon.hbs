<form class="{{cssClass}}" autocomplete="off">

    {{> systems/impmal/templates/items/partials/item-header.hbs isPhysical=isPhysical}}

    <section class="tab-section">
        <nav class="sheet-tabs tabs">
            <a class="tab-select item active" data-tab="notes">{{localize "IMPMAL.Description"}}</a>
            <a class="tab-select item" data-tab="details">{{localize "IMPMAL.Details"}}</a>
            <a class="tab-select item" data-tab="effects">{{localize "IMPMAL.Effects"}}</a>
        </nav>
        <div class="tab-content">
            <div class="tab notes" data-tab="notes">
                {{> systems/impmal/templates/items/partials/item-notes.hbs}}
            </div>
            <div class="tab details" data-tab="details">
                <div class="form-group">
                    <label>{{localize "IMPMAL.AttackType"}}</label>
                    <div class="form-fields">
                        <select name="system.attackType.value">
                            <option value="melee">Melee</option>
                            <option value="melee">Ranged</option>
                        </select>
                    </div>
                </div>
                <div class="form-group">
                    <label>{{localize "IMPMAL.Category"}}</label>
                    <div class="form-fields">
                        <select name="system.categroy.value">
                            <option value=""></option>
                            {{#if (eq system.attackType "melee")}}
                                {{#each (config "meleeTypes")}}
                                    <option value="{{@key}}">{{this}}</option>
                                {{/each}}
                            {{else}}
                                {{#each (config "rangedTypes")}}
                                    <option value="{{@key}}">{{this}}</option>
                                {{/each}}
                            {{/if}}
                        </select>
                    </div>
                </div>
                <div class="form-group">
                    <label>{{localize "IMPMAL.Specialisation"}}</label>
                    <div class="form-fields">
                        <select name="system.spec.value">
                            <option value=""></option>
                            {{#if (eq system.attackType "melee")}}
                                {{#each (config "meleeSpecs")}}
                                    <option value="{{@key}}">{{this}}</option>
                                {{/each}}
                            {{else}}
                                {{#each (config "rangedSpecs")}}
                                    <option value="{{@key}}">{{this}}</option>
                                {{/each}}
                            {{/if}}
                        </select>
                    </div>
                </div>

                {{> systems/impmal/templates/items/partials/item-damage.hbs}}

                <div class="form-group">
                    <label>{{localize "IMPMAL.Range"}}</label>
                    <div class="form-fields">
                        <select name="system.range">
                            <option value=""></option>
                            {{#each (config "ranges")}}
                                <option value="{{@key}}">{{this}}</option>
                            {{/each}}
                        </select>
                    </div>
                </div>
                <div class="form-group">
                    <label>{{localize "IMPMAL.Mag"}}</label>
                    <div class="form-fields">
                        <input type="number" name="system.mag" value="{{system.mag}}">
                    </div>
                </div>

                <div class="traits">

                </div>
            </div>
            <div class="tab effects" data-tab="effects">
                {{> systems/impmal/templates/items/partials/item-effects.hbs}}
            </div>
        </div>
    </section>
</form>