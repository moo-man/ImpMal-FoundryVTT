<div class="impmal reward">
    {{reason}}
    {{#if solars}}
    <p>Solars: {{solars}}</p>
    {{/if}}
    {{#if xp}}
    <p>XP: {{xp}}</p>
    {{/if}}
    <button type="button" data-action="receiveReward">{{localize "IMPMAL.Receive"}}</button>
</div>